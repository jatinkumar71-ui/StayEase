<form action="/favourites" method="POST">
    <button
        class="bg-blue-600 hover:bg-red-500 text-white px-8 py-3 rounded-lg text-lg font-semibold">
        Add To favourite
    </button>
    <input type="hidden" name="id" value="<%= home.id %>">
</form>